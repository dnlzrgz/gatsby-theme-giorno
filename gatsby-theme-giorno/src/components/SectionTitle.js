import React from 'react';

const SectionTitle = ({ children }) => (
	<div className="pt-12 px-3 mb-5">
		<h1 className="text-center text-3xl">{children}</h1>
	</div>
);

export default SectionTitle;
